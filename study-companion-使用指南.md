# Study Companion 使用指南

Study Companion（学习系统陪跑器）是一款在本地浏览器运行的学习计划工具。数据保存在当前浏览器的 localStorage 中，不会自动上传到服务器。

## 一、启动项目

### 环境要求

- Node.js 20.19+ 或 22.12+
- npm

### 首次启动

1. 打开终端。
2. 进入项目目录：

   ```bash
   cd ~/Desktop/study-companion
   ```

3. 安装依赖：

   ```bash
   npm install
   ```

4. 启动开发服务器：

   ```bash
   npm run dev
   ```

5. 在浏览器访问终端显示的地址（默认是 `http://localhost:3003`）。

停止服务时，在终端按 `Control + C`。

## 二、日常使用

1. 首次进入后，点击“创建学习周期”。
2. 设置周期名称、日期、学习日规则、启动暗号等。
3. 进入“目标”，添加至少一个学习目标。
4. 每个学习日回到首页：
   - 若开启健康前置，先完成健康检查或使用当日特例。
   - 输入启动暗号，生成今日计划。
5. 在“今日任务”中勾选任务，或填写完成数量。
6. 学习结束后填写状态和备注，点击“收工”。
7. 在“历史”和“复盘”中查看完成率、节奏和连续收工天数。

## 三、数据备份与恢复

### 导出备份

进入“设置” → “数据管理” → “导出 JSON”。建议定期备份，尤其是在清理浏览器数据或更换浏览器之前。

### 恢复数据

进入“设置” → “数据管理”，选择 JSON 文件导入，或粘贴 JSON 内容后导入。导入成功后刷新页面。

### 重要提醒

- 学习数据只保存在使用该应用的浏览器中。
- 清理浏览器站点数据会删除学习记录。
- 不同浏览器、隐私窗口和不同本地端口之间的数据默认不共享。

## 四、测试与构建

运行自动化测试：

```bash
npm test
```

生成生产版本：

```bash
npm run build
```

本地预览生产版本：

```bash
npm run preview
```

## 五、常见问题

### 页面没有生成任务

确认当前日期是学习日、已有激活目标，并输入了正确的启动暗号。

### 更换浏览器后记录不见了

这是本地存储的正常表现。回到原浏览器导出 JSON，再在新浏览器中导入。

### 端口被占用

可以指定其他端口启动：

```bash
npm run dev -- --port 3004
```

### 想完全重置

先导出备份，再到“设置” → “数据管理” → “清空所有本地数据”。此操作不可恢复。

