# 学习陪跑（study-companion）网页版重设计 Brief

> 给 Claude Design 用。它抓不到本地页面，所以这份文档 + `design-refs/` 里的整页截图
> 就是"现状的全部事实"。整段贴给它，再把截图一起上传。

---

## 1. 这是什么产品

单人用的**学习陪跑系统**：把长期目标拆成每天可承受的任务，按你的状态滚动调整，
晚上一次收工反馈。**纯前端、纯本地**（localStorage），无账号、无后端、无同步。
中英双语，桌面与移动都要用。

不是待办清单，不是番茄钟，**尤其不是打卡器**。

---

## 2. 核心理念（重设计中必须活下来的部分）

这几条是产品的灵魂，视觉与交互都要为它们服务：

1. **允许失败，不允许脱轨**——用"系统每天在运行"代替"靠意志力坚持"。
2. **完成保底任务 = 今天没有断线**。保底档是唯一的"成功线"，推荐/可选都是加分。
3. **未完成不滚存到明天**——绝不制造欠债雪球，第二天是重新规划，不是补作业。
4. **状态不好时系统自动降强度**，用户不需要为此解释或愧疚。
5. **每天最多一个主线目标**，避免多线并行内耗。
6. **0% 的一天、休息日，视觉上不能有任何惩罚感**——不用红色、不用"失败/中断"的措辞、
   不用下跌箭头。系统的态度是陪伴，不是监工。
7. **系统要解释自己的决定**：每天的计划都带一句"为什么这么排"（现状是页面顶部的
   蓝色 alert，例如"今日主线目标：政治·选择题。按正常节奏推进。"）。这是用户信任
   系统的唯一理由，不能缩成小灰字。
8. 页面底部**常驻一句话**，任何页面都在：
   > 开心和健康最重要，学习不是用来压迫自己和否定自己的。

语气基调：**温和、清爽、陪伴感、不刺眼**（这句话就写在现有 CSS 文件头部）。

---

## 3. 信息架构（现状）

左侧固定侧栏 240px + 右侧主内容区，底部常驻提醒条。

| 路由 | 页面 | 说明 |
|---|---|---|
| `/` | 首页 Dashboard | 无周期时是欢迎+使用说明；有周期时是当日入口 |
| `/cycle-setup` | 创建学习周期 | 低频、表单密集 |
| `/today` | 今日任务 | **主屏**，三档任务 + 收工前确认 |
| `/checkin/:planId` | 收工反馈 | 三个百分比 + 节奏判定 + 一句话建议 |
| `/goals` | 目标管理 | 增删改、进度条、激活/停用 |
| `/history` | 历史 | 按日期列出计划与收工记录 |
| `/review` | 复盘 | 周期级趋势 |
| `/settings` | 设置 | 特殊日标记、健康前置、暗号、数据导入导出 |

侧栏导航当前用 emoji 当图标：🏠 首页 / 📋 今日任务 / 🎯 目标 / 📅 历史 / 📊 复盘 / ⚙️ 设置。

---

## 4. 关键屏与关键状态

### 4.1 首页（有周期时）
- 标题：周期名 + `2026-08-28 · 第 1 天 · 学习日 · 周期进度 35.7%`
- **启动暗号**卡片：输入暗号才生成当日计划（默认暗号"开始学习"）。这是一个刻意的
  仪式感设计——每天亲手启动系统，而不是被系统推着走。目前视觉上完全没表达出这层含义。
- **健康前置**（可选开启）：必须先确认完成运动/睡眠等健康例行，才能启动学习计划。
  同样是理念级设计，目前只是一个普通勾选。
- 两个统计卡：周期总进度 / 激活目标数
- 最近 7 天节奏（现在是空占位）
- 可能出现的提醒条：完成率偏低已自动降强度 / 连续多天未收工 / 激活目标过多

### 4.2 今日任务（主屏，最重要）
- 顶部：`📋 今日任务` + `2026-08-28 · 正常模式`
- **计划理由** alert（蓝底）
- **今日主线目标** 卡片（badge 展示）
- **三档任务分组**，各有标题与颜色：
  - 🔹 **保底任务 — 完成就算今天没有断线**（主色深）
  - 📌 **推荐任务 — 正常状态时推进**（绿色）
  - 💡 **可选任务 — 状态好时再做，可不做**（灰）
- 每条任务：复选框 + 标题 + 目标量/单位 + 已完成量 + 数字输入框 + "跳过"按钮
- **收工前确认**卡片：今天的状态（😊好 / 😐一般 / 😔累 / 😞差）+ 备注 +
  客观阻断说明 + `✅ 收工 — 结束今天的学习`
- 提示：收工后当天结束，不再追加任务

### 4.3 收工反馈
三个并排百分比（今日完成 / 累计完成 / 应有进度）+ 节奏判定标签 + 总结 + 💬 一句话建议
+ 阻断说明。可设置为隐藏具体数量、只显示百分比（`hideRawAmountsInFeedback`，默认开）。

### 4.4 空态
每个页面都有空态（📚 / 📭 图标 + 标题 + 说明 + 主按钮）。空态不能有"你还没开始"的责备感。

---

## 5. 领域模型（决定界面需要表达哪些状态）

```
任务档位 TaskLevel      minimum 保底 / recommended 推荐 / optional 可选
任务状态 TaskStatus     notStarted / partial 部分 / completed / skipped 跳过 / blocked
当日状态 UserState      good 好 / normal 一般 / tired 累 / bad 差
节奏判定 RhythmStatus   ahead 超前 / stable 稳定 / slightlyBehind 略落后 / behind 落后 / slipping 脱轨中
当日模式 PlanMode       normal 正常 / light 轻量 / rest 休息 / holiday 放假 / exam 考试 / blocked 阻断
健康前置 HealthGate     pending / passed / exception 特例放行 / notRequired
目标类别 GoalCategory   course 课程 / problems 刷题 / memory 背诵 / reading 阅读 / project 项目 / custom
优先级/难度             high·medium·low / easy·normal·hard
```

**注意**：`slipping`（脱轨中）和 `bad`（状态差）这两个最"负面"的状态，恰恰最需要
温柔的视觉处理——它们出现时用户已经很难受了。

---

## 6. 现有视觉语言（`src/styles.css` 原样摘录）

```css
/* 主色调 - 柔和蓝绿 */
--color-primary: #5b9aa0;   --color-primary-light: #7db8be;
--color-primary-dark: #3e7a80;  --color-primary-bg: #e8f4f5;

/* 辅助色 */
--color-success: #6aaa8a;  --color-success-bg: #e8f5ed;
--color-warning: #c9a96e;  --color-warning-bg: #fef9ee;
--color-danger:  #c97e6e;  --color-danger-bg:  #fdf0ec;   /* 注意：是柔和陶土红，不是警报红 */
--color-info:    #6e9ec9;  --color-info-bg:    #eaf2fa;

/* 中性色 */
--color-bg: #faf9f7;        /* 暖白纸感背景 */
--color-surface: #ffffff;
--color-border: #e8e4df;    --color-border-light: #f0ede9;
--color-text: #2d2a26;      --color-text-secondary: #6b6560;  --color-text-muted: #9a938b;

/* 间距 0.25 / 0.5 / 1 / 1.5 / 2 / 3 rem */
/* 圆角 6 / 10 / 16 / 24 px */
/* 阴影 极淡：0 1px 3px rgba(0,0,0,.06) → 0 8px 30px rgba(0,0,0,.10) */
/* 字体 系统栈 + PingFang SC；正文 16px / 1.6 */
/* 动效 150ms / 250ms ease */
```

现有组件 class：`card` `card-title` `card-body` `btn btn-primary|secondary|success btn-sm|lg|block`
`badge badge-primary` `alert alert-info|success` `task-item` `stat-card` `percent-display`
`empty-state` `rhythm-indicator` `form-input|textarea|label|hint` `grid-3` `core-reminder`。

**这套色板本身是好的**（暖白纸 + 柔和蓝绿，完全没有刺眼色），可以保留精神内核，
但欢迎你重新演绎。

---

## 7. 想解决的问题（重设计的动机）

1. **emoji 当图标**：🏠📋🎯📅📊⚙️🔹📌💡 到处都是。跨平台渲染不一致、风格无法统一、
   显得廉价。需要一套真正的图标语言（线性图标 / 自绘符号皆可）。
2. **视觉层级太平**：全是等权重的白卡片，扫一眼找不到"今天到底该干嘛"。
   主屏应该在 1 分钟内让人知道当天要做什么。
3. **移动端塌陷粗糙**：侧栏变成横向 nav 后会被截断（见 `05-今日任务-移动端.png`，
   复盘/设置直接看不见了）。移动端需要真正的设计，不是缩窄的桌面版。
4. **仪式感没有被视觉表达**：启动暗号、健康前置是产品最有想法的两个设计，
   现在就是一个输入框和一个勾选框，完全没有"每天亲手启动系统"的感觉。
5. **"系统在运行"缺少可视化**：连续运行天数、节奏热力图这类能让人有安全感的元素还没有。
6. **收工反馈信息主次不清**：三个百分比等权并排，用户不知道该看哪个。
7. **没有深色模式**。
8. 任务的"部分完成"状态（partial）目前只靠一个数字输入框表达，缺少进度感。

---

## 8. 与已完成的 iOS 版（PLOS）的关系

同一个人的同一套学习理念，已有一个做完的原生 iOS App（暖绿色系、卡片、
"系统仍在运行"的休息日卡片）。网页版**可以呼应但不必照搬**——两者的产品形态不同：
iOS 版是每天两次的轻量入口，网页版信息更全（历史、复盘、目标管理）。

iOS 版主色供参考：主绿 `#4E8262`，纸背景 `#F6F8F2`，墨色 `#26302A`，
休息日陶土 `#A2825C`，低谷日青紫 `#7A80C4`，健康提醒琥珀 `#B4842F`。
**该设计明确规定：永不使用红色。**

---

## 9. 技术约束（设计必须落得下来）

- React 18 + TypeScript + Vite，React Router（**hash 路由**）
- **手写 CSS 变量，无 UI 框架、无 Tailwind、无组件库**——设计 token 要能直接映射成 CSS 变量
- 全部文案走 i18n key，**中英双语**，英文通常更长（布局要能容纳）
- 数据在 localStorage，**无网络请求**——不要设计任何"加载中/同步中"状态，计划是本地即时算出的
- 桌面 + 移动，同一份代码
- 无障碍：对比度达标、可键盘操作、`prefers-reduced-motion` 要尊重

---

## 10. 希望你产出什么

1. 先给 **2–3 个方向的视觉探索**（不同气质），锁定一个再展开。
2. 逐屏设计：首页（有/无周期）、今日任务（含三档任务与收工确认）、收工反馈、
   目标管理、设置、空态。
3. 移动端不是附属——今日任务与收工反馈至少要各出一版移动稿。
4. 一套图标语言，替换掉所有 emoji。
5. 设计 token（颜色/字号/间距/圆角/阴影/动效）命名规整，能直接落成 CSS 变量。
6. 明确标出**三档任务**、**五种节奏**、**四种状态**、**六种日模式**的视觉区分方式。
7. 深色模式的 token 值。

---

## 附：现状截图（`design-refs/`）

| 文件 | 内容 |
|---|---|
| `01-首页.png` | 有周期时的首页：启动暗号 + 统计卡 |
| `02-今日任务.png` | 主屏整页（桌面 1440px） |
| `03-目标管理.png` | 四个目标、进度条、类别与优先级 |
| `04-设置.png` | 特殊日、健康前置、暗号、数据管理 |
| `05-今日任务-移动端.png` | 390px 整页，含收工前确认；可看到侧栏塌陷的问题 |

截图里的数据是为了展示而造的样例（秋季考研冲刺周期 + 四个目标），非真实学习记录。
